export default interface IBookState {
    isLoading: boolean;
    error: string;
    isOpenModal: boolean;
    isActiveDropDown: boolean;
    isActiveInputBtn: boolean;
    isBookRow: boolean;
    isBookColumn: boolean;
}